# Template for local configuration. Copy to application-local.properties and adjust values.
# File: application-local.properties (same directory, gitignored)

spring.datasource.url=jdbc:postgresql://localhost:5432/recetario_db
spring.datasource.username=recetario_user
spring.datasource.password=your_password_here
