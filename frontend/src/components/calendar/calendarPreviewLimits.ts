/** Max meal categories shown per day in week preview (fills column, no scroll). */
export const WEEK_MEAL_LIMIT = 3;

/** Max recipe thumbnails per day cell in month preview. */
export const MONTH_RECIPE_LIMIT = 4;
